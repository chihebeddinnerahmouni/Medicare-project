

const PharmaciesCard = () => {
  return (
    // <div className={`card pt-[18px] flex flex-col items-center px-[18px] border-1 border-mainGreen bg-white absolute bottom-0 left-[50%] translate-x-[-50%] rounded-tr-20 rounded-tl-20 ${isCardDown ? `translate-y-[${translationValue}px]` : ''}`} style={{ width: 'calc(100% - 30px)' }}>
     <div>   
          
    </div>
  )
}

export default PharmaciesCard
